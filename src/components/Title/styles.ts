import { css } from '@emotion/core';

import { appBoldFont } from 'common/styles/global.styles';

export const title = [
  appBoldFont,
  css({
    margin: '56px 0 45px',
    color: '#475666',
    textAlign: 'center',
  }),
];

export default title;
